

import 'package:flutter/cupertino.dart';

class NoticeGuestPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('требуется авторизация'),
    );
  }
}